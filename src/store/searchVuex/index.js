// search 模块 vuex

import {
    getBaseCategoryList
} from '@/api';

const state = {
}

const mutations = {
   
}

const actions = {
    
}

const getters = {

}

export default {
    state,
    mutations,
    actions,
    getters
}